<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="003-create-txn-outbox-table" author="wisewallet-migrations" runOnChange="false" failOnError="true">
        <createTable tableName="txn_outbox" schemaName="transaction">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="aggregate_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="event_type" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="payload" type="JSONB">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(10)">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMPTZ" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
            <column name="processed_at" type="TIMESTAMPTZ">
                <constraints nullable="true"/>
            </column>
            <column name="retry_count" type="INT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>
            ALTER TABLE transaction.txn_outbox
                ADD CONSTRAINT chk_outbox_status
                    CHECK (status IN ('PENDING', 'SENT', 'FAILED'));
        </sql>

        <sql>
            CREATE INDEX idx_txn_outbox_status_created
                ON transaction.txn_outbox (status, created_at)
                WHERE status = 'PENDING';
        </sql>
    </changeSet>

</databaseChangeLog>
